namespace _3D_space
{
    using System;

    public class Point3DTest
    {
        // This class is for testing for 3DSpace
        // First comments others Main methods!!!
        /*static void Main()
        {
            Path newPath = PathStorage.LoadFile();
            Point3D testPoint = new Point3D(2, 5, 9);

            newPath.AddPoint(testPoint);

            foreach (var point in newPath.seqOfPoints)
            {
                Console.WriteLine(point.ToString());
            }

            PathStorage.SaveFile(newPath);

            Point3D firstPoint = new Point3D(4, 5, 6);
            Point3D secondPoint = new Point3D(19, 12, 18);

            Console.WriteLine(DistanseBetweenTwoPoints.CalculateDistance(firstPoint, secondPoint));
        }*/
    }
}
